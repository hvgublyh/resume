type ProjectData = {
  achievements: string[];
  demoUrl?: string;
  description: string;
  features: string[];
  id: string;
  name: string;
  repoUrl?: string;
}

export default ProjectData